import React from 'react';
import useTitle from '../hooks/useTitle';

const VeiwDetails = () => {
    useTitle("Details");
    return (
        <div>
            <h1>Veiw</h1>
        </div>
    );
};

export default VeiwDetails;